import {ReactNode} from 'react';

type IconProps = {
  size?: number | string
  width?: number | string
  height?: number | string
  icon?: ReactNode
}

const Icon = (props: IconProps) => {
  const {icon} = props

  return
}

export default Icon
